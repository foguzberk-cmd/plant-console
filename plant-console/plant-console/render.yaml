services:
  - type: web
    name: plant-console
    runtime: static
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
    # Single-page app: send any path back to index.html
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
